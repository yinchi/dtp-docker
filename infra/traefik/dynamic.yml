# Container-independent dynamic configuration for Traefik
http:
  middlewares:
    auth:
      forwardAuth:
        address: "http://auth:8000/users/me"
        trustForwardHeader: true
        authRequestHeaders:
        - "Authorization"
        authResponseHeaders:
        - "X-Auth-User-ID"
        - "X-Auth-Roles"
